# Fix the error

numbers = [1, 2, 3, 4, 5]
print("The sum is:", calculate_sum(numbers))

"""

# This program calculates the sum of all numbers in a list

def calculate_sum(numbers):
    total = 0
    for num in numbers:
        total += num
    return totals  # Deliberate error: 'totals' instead of 'total'

numbers = [1, 2, 3, 4, 5]
print("The sum is:", calculate_sum(numbers))


# Fix the error

numbers = [1, 2, 3, 4, 5]
print("The sum is:", calculate_sum(numbers))